# Core Dependencies for MkDocs PoC
# Install with: pip install -r requirements-core.txt
# Python version requirement: >=3.8,<3.14

# MkDocs core
mkdocs>=1.5.0,<2.0.0

# Markdown extensions (included in MkDocs but explicit for clarity)
# - toc: Table of contents with permalinks
# - tables: GitHub-flavored table support
# - fenced_code: Code blocks with syntax highlighting
# - admonition: Callout boxes (note, warning, tip, etc.)
# - attr_list: Add HTML/CSS attributes to Markdown elements

# Optional: Material theme (uncomment if desired)
# mkdocs-material>=9.0.0

# Python standard library dependencies (explicit for clarity)
pyyaml>=6.0
jinja2>=3.1.0

# HTTP and HTML processing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Utilities for professional runner
requests>=2.31.0  # For hosting validation tests
beautifulsoup4>=4.12.0  # For HTML validation
